import { API_URL } from "./config"

export const ENDPOINTS = {
    ROOT: `${API_URL}/`,
    STATUS: `${API_URL}/status`,
    GAMES: `${API_URL}/games`,
    ANALYTICS: `${API_URL}/analytics/dashboard`,
    FEATURED: `${API_URL}/games/featured`,
    SEARCH_GAME: `${API_URL}/search_game`
}
